# =============================================================================
# OpenClaw Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values.
# IMPORTANT: Never commit .env to version control!
#
# Usage:
#   cp config/.env.template config/.env
#   # Edit config/.env with your values
#   pnpm openclaw gateway
# =============================================================================


# =============================================================================
# PATHS & DIRECTORIES
# =============================================================================

# Agent workspace directory (where agent files are stored).
# If set to a relative path (e.g. "workspace"), the wrapper resolves it relative to the repo root.
# In this repo config, the default agent uses ${AGENT_WORKSPACE}/default.
AGENT_WORKSPACE=workspace

# OpenClaw state directory (credentials, sessions, logs, etc.).
# Leave blank to use the default (~/.openclaw). If set to a relative path (e.g. "bots"),
# the wrapper (`scripts/openclaw.mjs`) resolves it relative to the repo root.
OPENCLAW_STATE_DIR=

# OpenClaw config path (JSON5).
# Leave blank to use the default ($OPENCLAW_STATE_DIR/openclaw.json). If set to a relative path
# (e.g. "config/openclaw.json"), the wrapper resolves it relative to the repo root.
OPENCLAW_CONFIG_PATH=

# Browser executable path (Chromium/Chrome)
# Example: /home/dev/.cache/ms-playwright/chromium-1208/chrome-linux/chrome
BROWSER_EXECUTABLE_PATH=


# =============================================================================
# MODEL PROVIDERS
# =============================================================================

# NVIDIA Integrate API (for Kimi K2.5 model)
# Get your key at: https://build.nvidia.com/
NVIDIA_INTEGRATE_API_KEY=

# Gemini API (for memory search embeddings)
# Get your key at: https://aistudio.google.com/apikey
GEMINI_MEMORY_API_KEY=


# =============================================================================
# TOOLS & SERVICES
# =============================================================================

# Perplexity API (for web search)
# Get your key at: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# xAI API (for Grok web search)
# Get your key at: https://console.x.ai/
XAI_API_KEY=

# Exa API (for exa_search plugin)
# Get your key at: https://dashboard.exa.ai/api-keys
EXA_API_KEY=

# Moltbook API Key (for kimi agent)
# Register at: https://moltbook.com (see SKILL-moltbook.md)
MOLTBOOK_API_KEY=


# =============================================================================
# CHANNELS
# =============================================================================

# Telegram Bot Token
# Create a bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Telegram Bot Token (writer account)
# Used by channels.telegram.accounts.writer
TELEGRAM_BOT_TOKEN_WRITER=


# =============================================================================
# GATEWAY
# =============================================================================

# Gateway Authentication Token
# Generate a secure random token: openssl rand -hex 32
GATEWAY_AUTH_TOKEN=


# =============================================================================
# GOG (Google OAuth CLI)
# =============================================================================
# Leave blank if you don't use Google Calendar/Gmail integration.

# Config home directory for gogcli (where gog stores credentials/tokens)
# Leave blank unless you use GOG integration.
# If set to a relative path (e.g. "bots"), the wrapper resolves it relative to the repo root.
XDG_CONFIG_HOME=

# Keyring password for decrypting GOG OAuth tokens
# Get this from: cat <STATE_DIR>/.secrets/gog/keyring.pass
GOG_KEYRING_PASSWORD=

# Google account email for OAuth
GOOGLE_AUTH_EMAIL=
